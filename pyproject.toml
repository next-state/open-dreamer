[project]
name = "tiny-dreamer-4"
version = "0.1.0"
description = "Dreamer 4 implementation in pure Jax"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "einops",
    "flax",
    "hydra-core",
    "imageio[ffmpeg]",
    "jax[cuda12]",
    "nvidia-cusparse-cu12==12.5.8.93",  # pin to CUDA 12.8 compatible version; 12.5.10.65 requires nvJitLink 12.9
    "jaxlpips",
    "matplotlib",
    "optax>=0.2.8",
    "ott-jax",
    "tqdm",
    "wandb",
    "procgen-mirror; sys_platform != 'linux' or platform_machine != 'aarch64'",
    "tyro>=1.0.0",
    "array-record>=0.8.3",
    "grain>=0.2.15",
    "ipykernel>=7.1.0",
    "reactor-runtime>=0.0.4",
    "pandas>=2.3.3",
    "awscli>=1.27.0",
    "decord>=0.6.0; sys_platform != 'linux' or platform_machine != 'aarch64'",
    "ripgrep",
    "nvitop>=1.7.0",
]

[dependency-groups]
dev = [
    "ipdb>=0.13.13",
]


[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools]
package-dir = {"" = "."}
