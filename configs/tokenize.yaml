defaults:
  - dataset: minecraft_vpt
  - _self_

tokenizer_ckpt: /mnt/data/logs/tokenizer-big/checkpoints       # Required: path to pretrained tokenizer checkpoint
output_dir: /mnt/data/tokenized_data_last            # Required: output directory for latent shards
records_per_shard: 5000
seed: 0
parallel_strategy: data

hydra:
  run:
    dir: .
