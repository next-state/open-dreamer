defaults:
  - dataset: minecraft_vpt
  - _self_

tokenizer_ckpt: ???        # Required: path to pretrained tokenizer checkpoint
output_dir: ???            # Required: output directory for latent shards
batch_size: 8
num_workers: 16
records_per_shard: 1000
num_shards: null           # null = all shards
seed: 0
parallel_strategy: data

hydra:
  run:
    dir: .
