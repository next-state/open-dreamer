defaults:
  - dataset: bouncing_square
  - _self_

# Logging / Checkpointing
ckpt_max_to_keep: 2

# WandB
use_wandb: true
wandb_entity: null # add your wandb entity by adding `wandb_entity=<your_entity>` to the launch command
wandb_project: tiny_dreamer_4

# Hydra
hydra:
  run:
    dir: logs/${run_name}