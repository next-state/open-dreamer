defaults:
  - dataset: bouncing_square
  - _self_

# Logging / Checkpointing
ckpt_max_to_keep: 2

# WandB
use_wandb: true
wandb_entity: null
wandb_project: tiny_dreamer_4

# Hydra
hydra:
  run:
    dir: logs/${run_name}