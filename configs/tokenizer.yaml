defaults:
  - common
  - _self_

run_name: tokenizer
ckpt_max_to_keep: 5
ckpt_save_every: 10_000

patch_size: 4

lr: 1e-3
max_steps: 1_000_000_000
log_every: 100
lpips_weight: 0.2
lpips_frac: 0.5
visualize_every: 10_000