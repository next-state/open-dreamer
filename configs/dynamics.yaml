defaults:
  - common
  - model: dynamics
  - _self_

run_name: dynamics
tokenizer_dir: logs/tokenizer
ckpt_save_every: 50_000

dataset:
  B: 64

patch: 4

# probably this goes into dynamics.yaml
packing_factor: 2
n_register: 4
n_agent: 1

k_max: 8
bootstrap_start: 5_000
self_fraction: 0.25

max_steps: 1_000_000_000
log_every: 500
lr: 1e-3

write_video_every: 5_000