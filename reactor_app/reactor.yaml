model:
  name: world-model
  display-name: OpenDreamer World Model

runtime:
  import: pipeline:WorldModelPipeline
  config: config.yaml
  # Relative paths resolve against this reactor.yaml. Overridden by
  # $REACTOR_WEIGHTS_PATH at runtime if set.
  weights_path: ../jelly
